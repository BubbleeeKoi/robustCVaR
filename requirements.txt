# Runtime dependencies for the Figure 1 reproduction code.
# Install these in the `portfolio` conda environment before running scripts.
numpy
Pillow
matplotlib
pymupdf
aspose.slides

# Portfolio experiment (robust_cvar_portfolio/)
# conda activate portfolio
# pip install akshare pandas scipy pyyaml torch
